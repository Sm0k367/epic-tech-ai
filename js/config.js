// Epic Tech AI - Global Config

const APP_CONFIG = {
  version: "1.0.0",
  stripePublicKey: "", // Add Stripe publishable key here later
  apiBaseUrl: "",       // Optional: backend endpoint for LangChain or Assistants
  loadDelay: 100        // Delay between loading components (ms)
};
